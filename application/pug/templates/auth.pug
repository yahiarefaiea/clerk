#auth
  
  //- BACKGROUND
  .background
  
  //- INTRO
  #intro: .h3
    each line in auth.intro
      .spans= line
  
  //- SWITCH
  #switch
    .h3: .spans= auth.switch.headline
    ul.fields.blocks.inline.halves
      each option in auth.switch.options
        li: a.button.navigate(href='#' data-add=option.navigate.add
          data-remove=option.navigate.remove title=option.text)= option.text
    .note
      each line in auth.switch.note
        .small !{line}
  
  //- FORM
  mixin form(form)
    .h3: .spans= form.headline
    .note.small
      each line in form.note
        div= line
    .fields
      each field in form.fields
        +field(field.name, field.placeholder, false, field.type)
      if form.action.request
        a.button.request(href='#' + form.action.request
          title=form.action.text)= form.action.text
      if form.action.location
        a.button.navigate(href='#' data-add=form.action.navigate.add
          data-remove=form.action.navigate.remove title=form.action.text)= form.action.text
    .small: a.link.navigate(href='#' data-add=form.link.navigate.add
      data-remove=form.link.navigate.remove title=form.link.text)= form.link.text
  
  //- SIGN IN
  #signIn(data-request=auth.signIn.request)
    +form(auth.signIn)
